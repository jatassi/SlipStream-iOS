# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# SPM
.build/
.swiftpm/

# macOS
.DS_Store

# SDD scratch (subagent-driven-development progress ledger)
.superpowers/

# XcodeBuildMCP local session config (may hold dev creds) — never commit
.xcodebuildmcp/

# Per-package SPM lockfiles for local packages (the authoritative lockfile is
# the xcworkspace one: SlipStream.xcodeproj/.../swiftpm/Package.resolved).
Packages/*/Package.resolved
